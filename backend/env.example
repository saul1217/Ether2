# Puerto del servidor
PORT=3001

# Secret key para JWT (cambiar en producción)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# URL del RPC de Ethereum (opcional, usa public RPC por defecto)
ETHEREUM_RPC_URL=https://eth.llamarpc.com

# Dominios permitidos para CORS (separados por comas, opcional)
# Para permitir TODOS los dominios: ALLOWED_ORIGINS=*
# Para permitir dominios específicos: ALLOWED_ORIGINS=https://tu-pagina.com,https://pagina-amigo.com
# Si está vacío, permite todos en desarrollo. En producción, especifica los dominios.
ALLOWED_ORIGINS=*

# Ambiente (development o production)
NODE_ENV=development

# API Keys para controlar acceso (opcional, separadas por comas)
# Si está vacío, no se requiere API key
# API_KEYS=clave-1,clave-2,clave-3

